import { background } from './colors'

export const globalStyles = {
  backButtonStyle: {
    top: 0,
    left: 0,
    margin: '5%',
  },
  loadingSpinner: {
    display: 'block',
    margin: '0 auto',
    borderColor: background,
  },
}
